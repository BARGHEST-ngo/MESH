--- a/.golangci.yml
+++ b/.golangci.yml
@@ -90,5 +90,8 @@
       - path: util/singleflight/.*\.go
         text: File is not `gofmt`-ed with `-s` `-r 'interface{} -> any'`
+      # Upstream govet issue
+      - path: cmd/tailscale/cli/status\.go
+        text: "non-constant format string in call to f"
     paths:
       - third_party$
       - builtin$
